Put your custom `Command` classes here, one per file (e.g. `Shoot.java`, `StartIntake.java`), in the `robot.commands` package.

See [`examples/ftc/ExampleCommand.java`](../../../../examples/ftc/ExampleCommand.java) for a template
and [`framework/README.md`](../../../../framework/README.md) for the `Command` interface.
